#ifndef _MAIN_H_
#define _MAIN_H_
#include <stdarg.h>
typedef struct func_sel{
	char *c;
	char *(*func)(const char *, va_list *);
} s_func;
int _strlen(const char *s);
char *_strdup(const char *str);
int _printf(const char *format, ...);
#endif /* _MAIN_H_ */
